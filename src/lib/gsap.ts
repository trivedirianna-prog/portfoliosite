import gsap from "gsap";

/*
  Central GSAP entry point. Register any plugins here (e.g. Draggable for
  §7.2 object dragging) so the rest of the app imports gsap from this one
  module instead of the raw package.
*/

export { gsap };
