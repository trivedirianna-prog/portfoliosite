import "./Committees.css";

/* Placeholder object — card wallet metaphor (§8.4). No content/UI yet. */
export function Committees() {
  return (
    <div className="section-object section-object--committees material-paper">
      <span className="section-object__caption label-mono">Committees</span>
    </div>
  );
}
